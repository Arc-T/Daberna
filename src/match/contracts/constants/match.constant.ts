// src/match/contracts/constants/match.constant.ts

/** Docs page 16: 1–2s interval — from Remote Config */
export const CALL_INTERVAL_MS = 1500;

/** All 90 numbers */
export const ALL_NUMBERS = Array.from({ length: 90 }, (_, i) => i + 1);

/** New player (Daberna-2, page 24–25) */
export const NEW_PLAYER_FILLER_MIN = 20;
export const NEW_PLAYER_FILLER_MAX = 25;
